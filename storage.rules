rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // App does not use Firebase Storage — deny all access
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
